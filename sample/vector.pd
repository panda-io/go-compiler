namespace;

import console;
import collection;

var v collection.vector<int>;

function main() int
{
    console.write_line("hello world");
    v = create_vector();
    console.write_line(v.size());
    v.push(1);
    v.push(2);
    console.write_line(v.size());
    var x = v.pop();
    console.write_line(x);
    console.write_line(v.size());
    return 0;
}

function create_vector() collection.vector<int> 
{
    return new collection.vector<int>();
}